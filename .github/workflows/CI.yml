# This workflow will do a clean install of node dependencies, build the source code and run tests on Chrome Browser on our VM
name: Automation CI

on:
  #workflow_dispatch:
  push:
    branches: [ dev ]
  pull_request:
    branches: [ dev ]

jobs:
  CICD-bvt:

    runs-on: windows-latest

    steps:

    - uses: actions/checkout@v2   
    
    - run: echo "install protractor"
